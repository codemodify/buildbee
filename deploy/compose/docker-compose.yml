# Postgres 16 + BuildBee Server.
# From the repo root:
#   docker compose -f deploy/compose/docker-compose.yml up --build
# Optional Artifact object-storage stub:
#   docker compose -f deploy/compose/docker-compose.yml --profile extras up

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: buildbee
      POSTGRES_PASSWORD: buildbee
      POSTGRES_DB: buildbee
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U buildbee -d buildbee"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - postgres_data:/var/lib/postgresql/data

  server:
    build:
      context: ../..
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      BUILDBEE_ADDR: ":8080"
      # Server applies SQL migrations on process start.
      DATABASE_URL: postgres://buildbee:buildbee@postgres:5432/buildbee?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy

  # Sandbox supervisor. Mount the Docker socket to run real containers.
  #   docker compose -f deploy/compose/docker-compose.yml --profile runtime up --build
  runtime:
    profiles: ["runtime"]
    build:
      context: ../..
      dockerfile: Dockerfile.worker
    ports:
      - "8090:8090"
    environment:
      BUILDBEE_RUNTIME_ADDR: ":8090"
      BUILDBEE_URL: http://server:8080
      BUILDBEE_FAKE_SANDBOX: ${BUILDBEE_FAKE_SANDBOX:-1}
    depends_on:
      - server
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  # Optional later: Artifact object storage. Not required for the Server stub.
  minio:
    profiles: ["extras"]
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: buildbee
      MINIO_ROOT_PASSWORD: buildbee-minio
    ports:
      - "9000:9000"
      - "9001:9001"

volumes:
  postgres_data:
